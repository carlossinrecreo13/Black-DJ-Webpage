
var bouncer = function(agePerson){

  var minimumAge = 18;
  var response = "";


  if( agePerson > minimumAge) {
   response = "Adelante";
 } if (agePerson < minimunAge) {

    response = "Fuera";
  }
  let bouncer = function( agePerson);

let agesIn =;
  let bouncer = function( agesLine);

  let agesIn = [];
  let agesOut = [];

  for (var i = 0; i < agesLine.length; i++) {
  agesLine[i];

if (agesLine > minimumAge);
console.log();
  }


}
bouncer 15
